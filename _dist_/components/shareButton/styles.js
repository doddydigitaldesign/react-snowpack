import {createStyles, makeStyles} from "../../../web_modules/@material-ui/core.js";
export const useShareButtonStyles = makeStyles((theme) => createStyles({
  alignLeft: {
    marginLeft: "80%"
  }
}));
