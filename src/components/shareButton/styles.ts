import { createStyles, makeStyles, Theme } from '@material-ui/core';

export const useShareButtonStyles = makeStyles((theme: Theme) =>
  createStyles({
    alignLeft: {
      marginLeft: '80%',
    },
  }),
);
