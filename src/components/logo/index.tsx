import { Typography } from '@material-ui/core';
import React from 'react';

interface Props {}

export const Logo = (props: Props) => {
  return <Typography variant={'body1'}>DDD React Snowpack</Typography>;
};
